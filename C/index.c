#include <stdio.h>

int main(){
    int v[10];

    for(int i = 0; i <= 9; i++){
        scanf("%d", v[i]);
        if(){
            v[i] = 20;
        }
    }
    for(int i = 0; i <= 9; i++){
        printf("%d ", v[i]);
    }
}